<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- JDBC Database connection settings -->
        <property name="connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="connection.url">jdbc:mysql://localhost:3306/second?useSSL=false</property>
        <property name="connection.username">root</property>
        <property name="connection.password">B@%hG9j4nWOzF%bw</property>
        <!-- JDBC connection pool settings ... using built-in test pool -->
        <property name="connection.pool_size">10</property>
        <!-- Echo the SQL to stdout -->
        <property name="show_sql">true</property>
        <!-- Set the current session context -->
        <property name="current_session_context_class">thread</property>
        <!-- Drop and re-create the database schema on startup -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <mapping class="org.example.dataclasses.Course"></mapping>
        <mapping class="org.example.dataclasses.Student"></mapping>
        <mapping class="org.example.dataclasses.Subscription"></mapping>
        <mapping class="org.example.dataclasses.Teacher"></mapping>
        <mapping class="org.example.dataclasses.PurchaseList"></mapping>
        <mapping class="org.example.dataclasses.LinkedPurchaseList"></mapping>
    </session-factory>
</hibernate-configuration>
